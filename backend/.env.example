# 개발용 환경변수. NODE_ENV=production으로 실행할 때는 .env.production을 대신 읽는다
# (server.js 참고). 운영 환경변수 템플릿은 .env.production.example 참고.
NODE_ENV=development
DATABASE_URL=
JWT_ACCESS_SECRET=
JWT_REFRESH_SECRET=
PORT=
CORS_ORIGIN=
